# Optional dependencies for advanced features
# Install these after the main requirements.txt

# Community detection libraries that work with Python 3.12
python-louvain>=0.16
leidenalg>=0.10.0
igraph>=0.11.0

# Graph embedding libraries  
karateclub>=1.3.0
node2vec>=0.4.6

# PyTorch Geometric (install manually due to dependency issues)
# pip install torch-geometric
# pip install pyg_lib torch_scatter torch_sparse torch_cluster -f https://data.pyg.org/whl/torch-2.7.0+cpu.html

# Deep Graph Library (may have Python 3.12 issues)
# dgl>=1.0.0

# Alternative graph libraries that may not support Python 3.12
# cdlib>=0.3.0
# stellargraph>=1.2.1

# Graph analysis tools
graph-tool-stubs>=0.1.0
